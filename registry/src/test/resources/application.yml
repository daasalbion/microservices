server:
  port: 9000

spring:
  application:
    name: eureka
  security:
    user:
      name: admin
      password: admin
  boot:
    admin:
      client:
        enabled: false
  cloud:
    config:
        enabled: false

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false